import { AccessControlList } from './access-control-list';

describe('AccessControlList', () => {
  it('should create an instance', () => {
    expect(new AccessControlList()).toBeTruthy();
  });
});
