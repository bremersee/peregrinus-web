import { AccessControlEntry } from './access-control-entry';

describe('AccessControlEntry', () => {
  it('should create an instance', () => {
    expect(new AccessControlEntry()).toBeTruthy();
  });
});
